# 云诊易 MedFlow — 功能辅助表设计文档

## 与主业务表的区别

| | 主业务表 (`medflow.sql`) | 功能辅助表 (`medflow_func.sql`) |
|------|------|------|
| 作用 | 直接承载看病/挂号/开药流程 | 支撑系统运行（日志/验证/配置） |
| 与业务耦合 | 强依赖，表间外键密集 | 弱依赖，不设外键或仅弱引用 |
| 数据生命周期 | 永久保留 | 有限期保留或可清理 |

---

## F1. 操作日志表（audit_log）

记录系统中所有关键操作，满足医疗系统合规与审计追溯要求。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, 自增 | 主键 |
| user_id | BIGINT | | 操作人ID（不设外键，用户删除后日志不丢失） |
| user_name | VARCHAR(50) | | 操作人姓名（冗余，直接可读） |
| role | TINYINT | | 操作人角色：0=管理员 / 1=医生 / 2=病人 |
| action | VARCHAR(50) | NOT NULL | 操作类型，如 LOGIN / CREATE_APPOINTMENT / WRITE_DIAGNOSIS |
| target_type | VARCHAR(50) | | 操作对象类型，如 appointment / diagnosis_record |
| target_id | BIGINT | | 操作对象ID |
| old_value | VARCHAR(500) | | 修改前的值（JSON格式），新增操作填 NULL |
| new_value | VARCHAR(500) | | 修改后的值（JSON格式），删除操作填 NULL |
| detail | VARCHAR(500) | | 操作详情，如 "病人张三挂了内科王医生的号" |
| ip_address | VARCHAR(45) | | 操作IP |
| created_at | DATETIME | NOT NULL | 操作时间 |

**设计要点：**
- `user_id` 不设外键——用户被软删除或注销后，审计日志必须独立存在
- 冗余 `user_name` + `role`——查日志时无需 JOIN 用户表，直接可读
- `old_value` / `new_value` 存 JSON 格式的字段快照——追查问题时能精确看到改了什么数值
- 写入方式：后端通过中间件/装饰器统一拦截，业务代码无感知
- 保留策略：至少 3 年，可按年度归档到历史表

### 必须记录的操作清单（共 31 种 action）

**总原则：** 以下操作一经发生，必须写入 `audit_log`，不论操作人是管理员、医生还是病人，同等对待、无豁免。

命名规范：`{动词}_{对象}`，如 `CREATE_DEPARTMENT`、`VIEW_DIAGNOSIS`。

#### 认证安全（5 种）

| action | 说明 |
|------|------|
| `LOGIN_SUCCESS` | 登录成功 |
| `LOGIN_FAILED` | 登录失败（记录失败次数，用于风控） |
| `CHANGE_PASSWORD` | 修改密码 |
| `DISABLE_USER` | 管理员禁用用户 |
| `ENABLE_USER` | 管理员启用用户 |

#### 科室（3 种）

| action | 说明 |
|------|------|
| `CREATE_DEPARTMENT` | 新增科室 |
| `UPDATE_DEPARTMENT` | 编辑科室 |
| `DELETE_DEPARTMENT` | 软删除科室 |

#### 医生（3 种）

| action | 说明 |
|------|------|
| `CREATE_DOCTOR` | 新增医生 |
| `UPDATE_DOCTOR` | 编辑医生 |
| `DELETE_DOCTOR` | 软删除医生 |

#### 病人（3 种）

| action | 说明 |
|------|------|
| `CREATE_PATIENT` | 新增病人（注册时自动创建） |
| `UPDATE_PATIENT` | 编辑病人信息 |
| `DELETE_PATIENT` | 软删除病人（注销） |

#### 药品（4 种）

| action | 说明 |
|------|------|
| `CREATE_DRUG` | 新增药品 |
| `UPDATE_DRUG` | 编辑药品信息 |
| `DELETE_DRUG` | 软删除药品 |
| `UPDATE_DRUG_STOCK` | 调整库存——old_value/new_value 记录 stock 变化 |

#### 挂号（3 种）

| action | 说明 |
|------|------|
| `CREATE_APPOINTMENT` | 病人挂号 |
| `UPDATE_APPOINTMENT` | 病人修改挂号（改时间/改医生/改科室） |
| `CANCEL_APPOINTMENT` | 病人取消挂号 |

#### 诊断（2 种）

| action | 说明 |
|------|------|
| `CREATE_DIAGNOSIS` | 医生填写诊断记录 |
| `UPDATE_DIAGNOSIS` | 医生修改诊断记录（允许修改，操作日志保留原始值） |

#### 处方（2 种）

| action | 说明 |
|------|------|
| `CREATE_PRESCRIPTION` | 医生开具处方（含多条药品明细） |
| `UPDATE_PRESCRIPTION` | 医生修改处方——仅限订单状态为"待取药"或"已取消"时允许；处方明细先删后插 |

#### 排班（3 种）

| action | 说明 |
|------|------|
| `CREATE_SCHEDULE` | 管理员设置医生排班 |
| `UPDATE_SCHEDULE` | 修改排班（改时间/时段/限号数） |
| `DELETE_SCHEDULE` | 删除排班或标记停诊 |

#### 文件（2 种）

| action | 说明 |
|------|------|
| `UPLOAD_FILE` | 上传附件（检查报告/诊断附件） |
| `DELETE_FILE` | 删除附件 |

#### 订单（1 种）

| action | 说明 |
|------|------|
| `CANCEL_DRUG_ORDER` | 取消药品订单（退还库存） |

> 药品订单随处方自动生成并扣减库存，不产生独立的 action。

#### 隐私访问（3 种）

| action | 说明 |
|------|------|
| `VIEW_DIAGNOSIS` | 查看诊断记录——病人看自己、医生看病人、管理员审查，均记录 |
| `VIEW_PRESCRIPTION` | 查看处方记录——同上 |
| `VIEW_DRUG_ORDER` | 查看药品订单——同上 |

#### 不记录的操作

查询列表、页面刷新、无敏感数据的 GET 请求。

### old_value / new_value 示例

管理员将阿莫西林库存从 100 调为 80：
```
old_value = {"stock": 100}
new_value = {"stock": 80}
```

---

## F2. 验证码表（verification_code）

存储注册、登录、找回密码等场景的短信或邮箱验证码。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, 自增 | 主键 |
| target | VARCHAR(100) | NOT NULL | 接收方（手机号或邮箱） |
| code | VARCHAR(10) | NOT NULL | 验证码（6位数字） |
| scene | VARCHAR(30) | NOT NULL | 使用场景：REGISTER / LOGIN / RESET_PASSWORD |
| send_ip | VARCHAR(45) | | 发送请求的IP（用于防刷风控——同一IP短时间大量请求则拦截） |
| attempt_count | TINYINT | NOT NULL, DEFAULT 0 | 校验失败次数（≥3次即作废，防暴力破解） |
| expires_at | DATETIME | NOT NULL | 过期时间（发送后 5 分钟） |
| used | TINYINT | NOT NULL, DEFAULT 0 | 是否已使用：0=未使用 / 1=已使用 |
| created_at | DATETIME | NOT NULL | 发送时间 |

**设计要点：**
- 不引入 Redis，MySQL 足够支撑小项目初期的验证码需求
- 校验条件：`target=? AND code=? AND expires_at > NOW() AND used=0 AND attempt_count < 3`
- 风控双保险：`send_ip` 限制发送频率 + `attempt_count` 限制重试次数
- 定时任务清理过期数据：`DELETE WHERE expires_at < NOW()`

---

## F3. 系统配置表（system_config）

集中管理系统级参数，避免硬编码。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, 自增 | 主键 |
| config_key | VARCHAR(100) | UNIQUE, NOT NULL | 配置键，如 `default_appointment_slots` |
| config_value | VARCHAR(500) | | 配置值，如 `"08:00,10:00,14:00,16:00"` |
| description | VARCHAR(255) | | 说明，如 "默认挂号时段配置" |
| created_at | DATETIME | NOT NULL | 创建时间 |
| updated_at | DATETIME | NOT NULL | 修改时间 |

**典型配置项：**

| 分类 | config_key | config_value | 说明 |
|------|------|------|------|
| 挂号 | `default_appointment_slots` | 08:00,10:00,14:00,16:00 | 每天可预约时段 |
| 挂号 | `max_appointments_per_day` | 50 | 每位医生每日最大挂号数 |
| 挂号 | `appointment_cancel_window_hours` | 2 | 距预约开始几小时内不可取消挂号 |
| 验证码 | `verification_code_length` | 6 | 验证码位数 |
| 验证码 | `verification_code_ttl_min` | 5 | 验证码有效期（分钟） |
| 验证码 | `verification_code_max_attempts` | 3 | 验证码最大重试次数（超限作废） |
| 验证码 | `verification_code_send_interval_sec` | 60 | 同一邮箱两次发送间隔（秒） |
| 安全 | `max_login_attempts` | 5 | 登录失败最大次数（超限锁定） |
| 安全 | `login_lock_minutes` | 15 | 登录锁定时长（分钟） |
| 处方 | `prescription_edit_window_hours` | 24 | 处方开具后几小时内可修改 |
| 系统 | `system_name` | 云诊易 | 系统显示名称 |
| 系统 | `audit_log_retention_days` | 1095 | 操作日志保留天数（默认3年） |

> **注意：** 敏感信息（SMTP 密码、数据库密码、JWT 密钥等）不放此表，存 `.env` 环境变量。

**设计要点：**
- 键值对模式，新增配置项只需 INSERT 一行，不需要改表结构
- 后端启动时全量加载到内存，管理员修改后通过接口或重启刷新缓存

---

## F4. 数据字典表（data_dict）

统一管理系统中所有 TINYINT 枚举的含义。前端渲染下拉框、后端返回状态文本时，统一从此表读取，避免各端硬编码。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, 自增 | 主键 |
| dict_type | VARCHAR(50) | NOT NULL | 字典类型，如 `user_role` / `appointment_status` |
| dict_key | TINYINT | NOT NULL | 枚举值，如 0 / 1 / 2 |
| dict_label | VARCHAR(50) | NOT NULL | 显示文本，如 "管理员" / "已取消" |
| sort_order | INT | NOT NULL, DEFAULT 0 | 排序（数字小的排前面） |
| created_at | DATETIME | NOT NULL | 创建时间 |
| updated_at | DATETIME | NOT NULL | 修改时间 |

**设计要点：**
- 唯一约束 `(dict_type, dict_key)`，同一类型下枚举值不重复
- 后端启动时全量加载到内存，前端通过接口获取字典列表
- 新增枚举值时只需 INSERT 一行，不用改表结构

### 初始化数据

| dict_type | dict_key | dict_label | 说明 |
|------|------|------|------|
| `user_role` | 0 | 管理员 | - |
| `user_role` | 1 | 医生 | - |
| `user_role` | 2 | 病人 | - |
| `user_status` | 0 | 禁用 | - |
| `user_status` | 1 | 正常 | - |
| `gender` | 0 | 未知 | 病人性别 |
| `gender` | 1 | 男 | - |
| `gender` | 2 | 女 | - |
| `appointment_status` | 0 | 已取消 | - |
| `appointment_status` | 1 | 待就诊 | - |
| `appointment_status` | 2 | 已就诊 | - |
| `drug_order_status` | 0 | 已取消 | - |
| `drug_order_status` | 1 | 待取药 | - |
| `drug_order_status` | 2 | 已取药 | - |
| `yes_no` | 0 | 否 | 通用是否枚举 |
| `yes_no` | 1 | 是 | - |

---

## F5. 令牌黑名单表（token_blacklist）

JWT 本身是无状态的，用户登出后 token 仍然有效。此表通过记录已登出的 token 来实现真正的登出功能。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, 自增 | 主键 |
| token_jti | VARCHAR(100) | UNIQUE, NOT NULL | JWT 的 jti 声明值（唯一标识一个 token） |
| user_id | BIGINT | NOT NULL | 所属用户ID |
| expires_at | DATETIME | NOT NULL | token 原始过期时间（过期后可自动清理） |
| created_at | DATETIME | NOT NULL | 拉黑时间（即登出时间） |

**校验流程：**
```
请求携带 JWT → 解析 jti → 查 token_blacklist WHERE token_jti = ?
                                              ↓
                          命中 → 401 Unauthorized（已登出）
                          未命中 → 放行
```

**清理策略：** 定时任务 `DELETE WHERE expires_at < NOW()`。

---

## F6. 文件附件表（file_attachment）

病人上传检查报告、影像，医生上传诊断附件。文件存本地磁盘，数据库只存路径和元信息。仅允许图片（jpg/png/gif/webp）和视频（mp4/avi/mov），校验在应用层执行。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, 自增 | 主键 |
| uploader_id | BIGINT | NOT NULL | 上传人ID |
| uploader_role | TINYINT | NOT NULL | 上传人角色（取值同 `user_role` 字典）：0=管理员 / 1=医生 / 2=病人 |
| related_type | VARCHAR(50) | NOT NULL | 关联对象，如 `diagnosis_record` / `prescription` |
| related_id | BIGINT | NOT NULL | 关联对象ID |
| file_name | VARCHAR(255) | NOT NULL | 原始文件名 |
| file_path | VARCHAR(500) | NOT NULL | 存储路径 |
| file_size | BIGINT | | 文件大小（字节） |
| file_type | VARCHAR(100) | | MIME 类型，如 `image/png` |
| created_at | DATETIME | NOT NULL | 上传时间 |

**设计要点：**
- 不设外键到 `user` 表，用 `uploader_id` + `uploader_role` 冗余，同 audit_log 的思路
- `related_type` + `related_id` 联合定位附件属于哪条业务记录
- 实际文件存储路径由后端根据规则生成，不在数据库暴露

---

## F7. 通知消息表（notification）

系统在关键业务节点自动生成通知，用户在各自前端查看。

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | BIGINT | PK, 自增 | 主键 |
| user_id | BIGINT | NOT NULL | 接收人ID |
| title | VARCHAR(100) | NOT NULL | 标题，如 "挂号提醒" |
| content | VARCHAR(500) | NOT NULL | 内容，如 "张三预约了您 2026-07-10 09:00 就诊" |
| type | VARCHAR(30) | NOT NULL | 通知分类：`APPOINTMENT`=挂号通知 / `DISPENSE`=诊疗取药通知 / `SYSTEM`=系统公告 |
| is_read | TINYINT | NOT NULL, DEFAULT 0 | 已读状态：0=未读 / 1=已读 |
| related_type | VARCHAR(50) | | 关联对象类型（点击通知可跳转到对应页面） |
| related_id | BIGINT | | 关联对象ID |
| created_at | DATETIME | NOT NULL | 发送时间 |

**自动触发场景：**

| 触发事件 | 通知对象 | 类型 | 示例内容 |
|------|------|------|------|
| 病人挂号成功 | 被挂号的医生 | `APPOINTMENT` | "张三预约了您 2026-07-10 09:00 就诊" |
| 病人取消挂号 | 被挂号的医生 | `APPOINTMENT` | "张三已取消 2026-07-10 09:00 的预约" |
| 医生排班停诊 | 已挂号该时段的病人 | `APPOINTMENT` | "王医生 2026-07-10 上午停诊，您的挂号已自动取消" |
| 医生填写诊断 | 病人 | `DISPENSE` | "王医生已完成您的诊断，请查收处方" |
| 医生修改诊断 | 病人 | `DISPENSE` | "王医生修改了您的诊断记录" |
| 处方生成订单 | 病人 | `DISPENSE` | "您的药品订单已生成，请到药房取药" |
| 订单已取药 | 病人 | `DISPENSE` | "您的药品已取药完成" |
| 订单被取消 | 病人 | `DISPENSE` | "您的药品订单已被取消" |
| 管理员发公告 | 指定用户或全体 | `SYSTEM` | 系统维护通知 |

---

## 已决事项

| 事项 | 决定 |
|------|------|
| system_config 管理方式 | MedFlow-admin 前端管理页面可视化编辑 |
| audit_log 存储 | 与业务表共用一个库，不做物理隔离（MVP 阶段数据量可控） |
| 文件存储方案 | 本地磁盘，`file_path` 存相对路径 |
