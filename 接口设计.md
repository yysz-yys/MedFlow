# 云诊易 MedFlow — 后端接口设计

## 项目结构

```
MedFlow-backend/
├── app/
│   ├── __init__.py
│   ├── main.py                     # FastAPI 入口，注册路由、中间件、定时任务
│   │
│   ├── core/                       # ===== 公共基础模块 =====
│   │   ├── __init__.py
│   │   ├── config.py               # 读取 .env 配置（数据库/SMTP/JWT密钥等）
│   │   ├── security.py             # JWT 生成/校验 + bcrypt 密码哈希/校验
│   │   ├── database.py             # SQLAlchemy 异步引擎 + 会话工厂 + Base
│   │   ├── sysconfig.py            # 业务配置缓存（从 system_config 表加载）
│   │   └── deps.py                 # 公共依赖注入（get_db / get_current_user / require_role）
│   │
│   ├── models/                     # ===== ORM 模型（18张表，每表一个文件）=====
│   │   ├── __init__.py             # 导出所有模型，方便统一 import
│   │   ├── base.py                 # 公共基类（id / created_at / updated_at）
│   │   ├── user.py                 # user 用户表
│   │   ├── department.py           # department 科室表
│   │   ├── doctor.py               # doctor 医生表
│   │   ├── doctor_schedule.py      # doctor_schedule 排班表
│   │   ├── patient.py              # patient 病人表
│   │   ├── drug.py                 # drug 药品表
│   │   ├── appointment.py          # appointment 挂号表
│   │   ├── diagnosis_record.py     # diagnosis_record 诊断记录表
│   │   ├── prescription.py         # prescription 处方表
│   │   ├── prescription_item.py    # prescription_item 处方明细表
│   │   ├── drug_order.py           # drug_order 药品订单表
│   │   ├── audit_log.py            # audit_log 操作日志表
│   │   ├── verification_code.py    # verification_code 验证码表
│   │   ├── system_config.py        # system_config 系统配置表
│   │   ├── data_dict.py            # data_dict 数据字典表
│   │   ├── token_blacklist.py      # token_blacklist 令牌黑名单表
│   │   ├── file_attachment.py      # file_attachment 文件附件表
│   │   └── notification.py         # notification 通知消息表
│   │
│   ├── schemas/                    # ===== Pydantic 请求/响应模型 =====
│   │   ├── __init__.py
│   │   ├── common.py               # 公共 schema（分页参数、通用响应）
│   │   ├── auth.py                 # 注册/登录/验证码 的请求体和响应体
│   │   ├── user.py                 # 用户相关
│   │   ├── department.py           # 科室相关
│   │   ├── doctor.py               # 医生相关
│   │   ├── doctor_schedule.py      # 排班相关
│   │   ├── patient.py              # 病人相关
│   │   ├── drug.py                 # 药品相关
│   │   ├── appointment.py          # 挂号相关
│   │   ├── diagnosis_record.py     # 诊断相关
│   │   ├── prescription.py         # 处方相关
│   │   ├── drug_order.py           # 订单相关
│   │   ├── file_attachment.py      # 文件相关
│   │   ├── notification.py         # 通知相关
│   │   ├── system_config.py        # 配置相关
│   │   ├── data_dict.py            # 字典相关
│   │   └── audit_log.py            # 日志相关
│   │
│   ├── api/                        # ===== 路由处理 =====
│   │   ├── __init__.py
│   │   └── v1/
│   │       ├── __init__.py         # 注册所有路由到 APIRouter
│   │       ├── auth.py             # 认证（注册/登录/登出/验证码）
│   │       ├── users.py            # 用户管理
│   │       ├── departments.py      # 科室 CRUD
│   │       ├── doctors.py          # 医生 CRUD
│   │       ├── doctor_schedules.py # 排班 CRUD
│   │       ├── patients.py         # 病人 CRUD
│   │       ├── drugs.py            # 药品 CRUD + 库存
│   │       ├── appointments.py     # 挂号 CRUD + 取消 + 就诊完成
│   │       ├── diagnosis_records.py# 诊断 CRUD
│   │       ├── prescriptions.py    # 处方 CRUD
│   │       ├── drug_orders.py      # 订单列表 + 取药 + 取消
│   │       ├── files.py            # 文件上传/下载/删除
│   │       ├── notifications.py    # 通知列表 + 未读数 + 已读
│   │       ├── data_dict.py        # 数据字典查询
│   │       ├── system_config.py    # 系统配置查询/更新
│   │       └── audit_logs.py       # 操作日志查询
│   │
│   ├── services/                   # ===== 业务逻辑层 =====
│   │   ├── __init__.py
│   │   ├── auth_service.py         # 注册/登录/验证码 业务逻辑
│   │   ├── user_service.py         # 用户管理
│   │   ├── appointment_service.py  # 挂号（含排班校验、时段限号）
│   │   ├── diagnosis_service.py    # 诊断（含挂号状态联动）
│   │   ├── prescription_service.py # 处方（含明细先删后插、订单生成）
│   │   ├── drug_order_service.py   # 订单（取药扣库存、取消）
│   │   ├── notification_service.py # 通知自动推送
│   │   └── file_service.py         # 文件保存/删除
│   │
│   ├── middleware/                  # ===== 中间件 =====
│   │   ├── __init__.py
│   │   ├── auth.py                 # JWT 解析 + token_blacklist 校验
│   │   └── audit.py                # 操作日志拦截（写操作自动记录）
│   │
│   └── utils/                      # ===== 工具 =====
│       ├── __init__.py
│       ├── email.py                # SMTP 发送邮箱验证码
│       ├── file_upload.py          # 文件类型校验 + 本地存储
│       └── exceptions.py           # 全局异常处理 + 统一错误码
│
├── tasks/                          # ===== 定时任务 =====
│   ├── __init__.py
│   ├── scheduler.py                # APScheduler 启动入口
│   └── cleanup.py                  # 清理过期验证码/过期token/旧日志
│
├── alembic/                        # ===== 数据库迁移 =====
│   ├── env.py
│   └── versions/
│
├── uploads/                        # 上传文件本地存储目录
│
├── alembic.ini                     # alembic 配置
├── .env.example                    # 环境变量模板
├── .gitignore
└── requirements.txt                # pip 依赖清单
```

各目录文件统计：

| 目录 | 文件数 | 说明 |
|------|------|------|
| `core/` | 5 | 配置/安全/数据库/sysconfig/依赖注入 |
| `models/` | 20 | base + 18 表 + init |
| `schemas/` | 17 | common + 16 模块 |
| `api/v1/` | 16 | 16 个路由模块 |
| `services/` | 8 | 业务逻辑层 |
| `middleware/` | 2 | JWT校验 + 操作日志 |
| `utils/` | 3 | 邮件/文件/异常 |
| `tasks/` | 2 | 调度 + 清理 |
| **合计** | **81** | 含配置文件和初始化文件 |

## 公共模块一览

| 模块 | 作用 |
|------|------|
| `core/config.py` | 统一管理所有配置，读 `.env`，一个 `Settings` 对象全局使用 |
| `core/security.py` | `hash_password()` / `verify_password()` / `create_token()` / `decode_token()` |
| `core/database.py` | `create_async_engine()` + `async_sessionmaker()`，所有接口通过 `get_db()` 拿会话 |
| `core/deps.py` | `get_current_user()` 从 JWT 解析当前用户；`require_role()` 限制管理员/医生/病人 |
| `middleware/auth.py` | 拦截请求→解析JWT→查 token_blacklist→放行或401 |
| `middleware/audit.py` | 拦截写操作→写入 audit_log |
| `utils/email.py` | `send_code(to_email)` 发验证码，含 SMTP 连接和模板 |
| `utils/file.py` | `save_upload(file)` 校验类型+存本地+返回路径 |
| `utils/exceptions.py` | 统一错误码（400/401/403/404/500） |
| `tasks/cleanup.py` | 定时清理：过期验证码 / token_blacklist 过期记录 / audit_log 超3年 |
| `tasks/scheduler.py` | APScheduler 启动入口 |

---

## 接口清单（16 模块，63 个接口）

### 1. 认证（auth）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `POST` | `/api/v1/auth/register` | 邮箱注册 | 无 |
| `POST` | `/api/v1/auth/send-code` | 发送验证码（注册/登录/找回密码） | 无 |
| `POST` | `/api/v1/auth/login` | 邮箱+验证码/密码登录，返回 JWT | 无 |
| `POST` | `/api/v1/auth/logout` | 登出，token 写入黑名单 | 登录 |
| `GET` | `/api/v1/auth/me` | 获取当前用户信息 | 登录 |
| `PUT` | `/api/v1/auth/me` | 修改个人信息（姓名/手机） | 登录 |
| `PUT` | `/api/v1/auth/password` | 修改密码 | 登录 |

### 2. 用户管理（users）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/users` | 用户列表（?role=&status=&keyword=） | 管理员 |
| `GET` | `/api/v1/users/{id}` | 用户详情 | 管理员 |
| `PUT` | `/api/v1/users/{id}/status` | 禁用/启用用户 | 管理员 |
| `POST` | `/api/v1/users/{id}/reset-password` | 重置密码 | 管理员 |

### 3. 科室（departments）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/departments` | 科室列表 | 登录 |
| `POST` | `/api/v1/departments` | 新增科室 | 管理员 |
| `PUT` | `/api/v1/departments/{id}` | 编辑科室 | 管理员 |
| `DELETE` | `/api/v1/departments/{id}` | 软删除科室 | 管理员 |

### 4. 医生（doctors）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/doctors` | 医生列表（?department_id=&name=） | 登录 |
| `POST` | `/api/v1/doctors` | 新增医生（含 user + doctor 创建） | 管理员 |
| `PUT` | `/api/v1/doctors/{id}` | 编辑医生 | 管理员 |
| `DELETE` | `/api/v1/doctors/{id}` | 软删除医生 | 管理员 |

### 5. 排班（doctor-schedules）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/doctor-schedules` | 排班列表（?doctor_id=&work_date_from=&work_date_to=） | 登录 |
| `POST` | `/api/v1/doctor-schedules` | 新增排班 | 管理员 |
| `PUT` | `/api/v1/doctor-schedules/{id}` | 编辑排班 | 管理员 |
| `DELETE` | `/api/v1/doctor-schedules/{id}` | 删除/停诊 | 管理员 |

### 6. 病人（patients）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/patients` | 病人列表 | 管理员 |
| `GET` | `/api/v1/patients/{id}` | 病人详情（含过敏史等） | 本人 / 该病人挂过号的医生 / 管理员 |
| `PUT` | `/api/v1/patients/{id}` | 编辑病人信息 | 管理员/本人 |
| `DELETE` | `/api/v1/patients/{id}` | 软删除（注销） | 管理员 |

### 7. 药品（drugs）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/drugs` | 药品列表（?name=&manufacturer=） | 登录 |
| `POST` | `/api/v1/drugs` | 新增药品 | 管理员 |
| `PUT` | `/api/v1/drugs/{id}` | 编辑药品 | 管理员 |
| `DELETE` | `/api/v1/drugs/{id}` | 软删除药品 | 管理员 |
| `PUT` | `/api/v1/drugs/{id}/stock` | 手动调整库存（进货补库存 / 盘点纠错） | 管理员 |

> **自动扣库存：** 处方创建时，后端在生成订单的同时自动扣减库存，不通过此接口。

### 8. 挂号（appointments）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/appointments` | 挂号列表（?status=&patient_id=&doctor_id=） | 病人看自己/医生看自己/管理员看全部 |
| `POST` | `/api/v1/appointments` | 挂号（选科室→选医生→选排班时段→提交） | 病人 |
| `PUT` | `/api/v1/appointments/{id}` | 修改挂号（重新选医生/时段） | 病人（本人） |
| `POST` | `/api/v1/appointments/{id}/cancel` | 取消挂号 | 病人（本人） |
| `POST` | `/api/v1/appointments/{id}/complete` | 就诊完成（标记已就诊） | 医生（该挂号的医生本人） |

### 9. 诊断记录（diagnosis-records）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/diagnosis-records` | 诊断列表（?patient_id=&doctor_id=） | 病人看自己/医生看自己/管理员 |
| `GET` | `/api/v1/diagnosis-records/{id}` | 诊断详情（含附件列表） | 病人本人 / 该诊断的医生本人 / 管理员 |
| `POST` | `/api/v1/diagnosis-records` | 新增诊断（挂号→已就诊→写诊断） | 医生 |
| `PUT` | `/api/v1/diagnosis-records/{id}` | 修改诊断记录 | 医生（本人） |

### 10. 处方（prescriptions）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/prescriptions` | 处方列表（?patient_id=&doctor_id=） | 病人看自己/医生看自己/管理员 |
| `GET` | `/api/v1/prescriptions/{id}` | 处方详情（含药品明细列表） | 病人本人 / 该处方的医生本人 / 管理员 |
| `POST` | `/api/v1/prescriptions` | 开具处方（含多条药品明细） | 医生 |
| `PUT` | `/api/v1/prescriptions/{id}` | 修改处方（订单未取药时允许） | 医生（本人） |

### 11. 药品订单（drug-orders）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/drug-orders` | 订单列表（?patient_id=&doctor_id=） | 病人看自己/医生看自己/管理员看全部 |
| `POST` | `/api/v1/drug-orders/{id}/cancel` | 取消订单（退还库存） | 病人(本人)/管理员 |
| `POST` | `/api/v1/drug-orders/{id}/complete` | 标记取药完成 | 病人(本人)/管理员 |

> **订单生成与库存扣减：** 处方创建时，后端自动生成 `drug_order`（status=1=待取药）并扣减药品库存。

### 12. 文件（files）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `POST` | `/api/v1/files/upload` | 上传文件（form-data，含 related_type/id） | 登录 |
| `GET` | `/api/v1/files/{id}/download` | 下载/查看文件 | 上传人 / 关联业务的医生与病人 / 管理员 |
| `DELETE` | `/api/v1/files/{id}` | 删除文件 | 上传人 / 管理员 |

### 13. 通知（notifications）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/notifications` | 我的通知列表（分页，自动过滤当前用户） | 登录 |
| `GET` | `/api/v1/notifications/unread-count` | 未读通知数 | 登录 |
| `POST` | `/api/v1/notifications` | 管理员发公告（可指定用户或全员） | 管理员 |
| `PUT` | `/api/v1/notifications/{id}/read` | 标记已读 | 登录（本人） |

> **推送规则：** `SYSTEM` 公告写入所有用户的 `notification` 各一条；`APPOINTMENT` 和 `DISPENSE` 仅写入相关单人。`GET` 接口服务层固定 `WHERE user_id = 当前用户`，各看各的。

### 14. 数据字典（data-dict）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/data-dict` | 获取全部字典（?type=user_role） | 无 |
| `POST` | `/api/v1/data-dict` | 新增字典条目 | 管理员 |
| `PUT` | `/api/v1/data-dict/{id}` | 编辑字典条目 | 管理员 |
| `DELETE` | `/api/v1/data-dict/{id}` | 删除字典条目 | 管理员 |

### 15. 系统配置（system-config）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/system-config` | 获取全部配置（前端各处需要，如挂号时段） | 无 |
| `POST` | `/api/v1/system-config` | 新增配置项 | 管理员 |
| `PUT` | `/api/v1/system-config/{id}` | 更新某项配置 | 管理员 |
| `DELETE` | `/api/v1/system-config/{id}` | 删除配置项 | 管理员 |

### 16. 操作日志（audit-logs）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| `GET` | `/api/v1/audit-logs` | 日志列表（?user_id=&action=&date_from=&date_to=） | 管理员 |

---

## 统计

| 模块 | 接口数 |
|------|------|
| 认证 | 7 |
| 用户管理 | 4 |
| 科室 | 4 |
| 医生 | 4 |
| 排班 | 4 |
| 病人 | 4 |
| 药品 | 5 |
| 挂号 | 5 |
| 诊断记录 | 4 |
| 处方 | 4 |
| 药品订单 | 3 |
| 文件 | 3 |
| 通知 | 4 |
| 数据字典 | 4 |
| 系统配置 | 4 |
| 操作日志 | 1 |
| **合计** | **64** |

---

## 权限体系说明

| 权限级别 | 含义 | 涉及接口数 |
|------|------|------|
| 无 | 不需要登录即可调用 | 4（注册/登录/验证码/字典） |
| 登录 | 携带有效 JWT，不限角色 | 20 |
| 病人 | 仅病人角色 `require_role([2])` | 1 |
| 病人(本人) | 病人 + 只能操作自己的数据 | 3 |
| 医生 | 仅医生角色 `require_role([1])` | 2 |
| 医生(本人) | 医生 + 只能操作自己创建的数据 | 6 |
| 管理员 | 仅管理员角色 `require_role([0])` | 22 |

**服务层按角色过滤（标注"登录"但返回数据自动裁剪）：**

| 接口 | 病人看到 | 医生看到 | 管理员看到 |
|------|------|------|------|
| `GET /appointments` | 自己的挂号 | 自己的挂号 | 全部 |
| `GET /diagnosis-records` | 自己的诊断 | 自己的诊断 | 全部 |
| `GET /prescriptions` | 自己的处方 | 自己的处方 | 全部 |
| `GET /drug-orders` | 自己的订单 | 医生自己处方的订单 | 全部 |
| `GET /notifications` | 自己的通知 | 自己的通知 | 全部 |
